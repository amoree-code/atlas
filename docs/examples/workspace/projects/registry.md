# Project registry

Index only. Detail lives in each project's own context file under `projects/` or in the
repo itself. Nothing here is a copy of source code.

**Status:** `active` (worked in the last ~2 months) · `warm` (this year, paused) ·
`dormant` (no commits in a year) · `vendor` (someone else's repo, cloned for reference)

| Project | Path | Stack | Repo | Status | Last | Next action |
|---|---|---|---|---|---|---|
| a-project | `~/code/a-project` | TypeScript · Node · Postgres | `example-org/a-project` | active | 2026-08-01 | ship the CSV export ticket, `EXMPL-001` |

This is the filled-in counterpart of `templates/workspace/projects/registry.md`, whose
example row you delete once you have a real one. `a-project`'s own ticket and context
files are worked out in full in `docs/examples/project/`.
