# Known-benign strings for `atlas privacy-scan`.
# One literal substring per line. A line matching means: this finding is noise.
# Keep this short. Every entry is a hole in the scan — justify it in a comment.

# Documentation placeholders.
example.com
user@example.com
you@example.com
/Users/you/
/home/you/
/Users/<you>/
<your-name>
your-name@example.com

# git's own no-reply form, used for repo-local public identity.
users.noreply.github.com

# The scanner and the policies that define the patterns describe them in prose;
# their own text must not count as a finding.
internal/governance/policies/privacy-classification.yaml
internal/governance/policies/privacy-allowlist.txt
cli/atlas-privacy-scan

# These tests deliberately feed credential-shaped fixtures (fake AWS key id, fake
# Anthropic key) into the mission/coordinator "refuse credential-shaped input" checks —
# the fixture matching the same pattern the scanner looks for is the point of the test.
tests/test-coordinator-conflict-protection.py
tests/test-mission-contract.py
