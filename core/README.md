# Core

There is no separate `core/` implementation yet — this directory exists as a stable place
to point at. Core today is implemented across `cli/` and specified by `schemas/`.

Read **[docs/design/core.md](../docs/design/core.md)** for what Core owns, what it
deliberately doesn't, and why the concept and the code aren't in the same directory yet.
