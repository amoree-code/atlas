# Your identifying terms — PRIVATE. Used by `ai-os privacy-scan` to check that the public
# Atlas repository does not contain anything that identifies you.
#
# This file never leaves ~/.ai-os. The public repository ships only generic patterns
# (key shapes, email shapes, home-path shapes) — a scanner that hardcoded your real name
# and employer would itself be the leak it exists to prevent. That is why the terms live
# here instead.
#
# One literal string per line, case-insensitive, minimum 3 characters.
# '#' starts a comment. Delete the examples and add your own.
#
# Worth listing:
#   your full name, and every spelling of it you use
#   your username / the account name in your home directory path
#   your account handles (GitHub, GitLab, npm)
#   your email addresses
#   your phone number
#   employers and clients
#   private or client repository names
#   internal hostnames and private URLs
#
# NOT worth listing: anything under 3 characters, or so common it appears in ordinary
# prose. Every noisy term makes the scan less likely to be run, which protects nothing.

# your-full-name
# your-github-handle
# your-employer
# a-private-repo-name
