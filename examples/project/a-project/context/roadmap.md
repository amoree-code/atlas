# a-project — where it is going

The direction and sequence of upcoming work, at the level a new contributor needs before
opening a ticket. Not a task list — that's the board in `../index.md`.

Example content for this fictional project:

1. CSV export (`EXMPL-001`) — ships first; smallest, reuses an existing endpoint.
2. Scheduled exports (emailed on a cadence) — depends on (1) landing and on an existing
   notifications capability being available.
3. XLSX as a second export format — only if a real consumer asks for it.
