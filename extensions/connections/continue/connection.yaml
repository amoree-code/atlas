connection: continue
name: Continue
contract: 1
aliases: [.continue]
client:
  detect: [~/.continue/, ~/.continue/config.yaml]
transport:
  preferred: mcp
  fallback: mcp
  gateway: atlas-mcp-gateway
