adapter: atlas-fixture
name: Atlas disposable fixture provider
contract: 1

client:
  detect: [/nonexistent/fake/atlas-fixture-binary]
  version_cmd: atlas-fixture --version
  consumer_verified: false

provides: {}
writes: []
requires: []
enforces: []

notes: >
  Disposable manifest-only provider for registry tests. It has no writable capabilities and
  no verified transport; any execution request must fail closed before subprocess dispatch.
