adapter: claude-code-mission-pilot
name: Claude Code CLI, bounded mission-scoped Read/Edit pilot
contract: 1

client:
  detect: [~/.local/bin/claude, ~/.claude/]
  version_cmd: claude --version
  consumer_verified: true

provides: {}
writes: []
requires: []
enforces: []

notes: >
  Transport-only identity. The verified transport registry owns the bounded argv,
  mission-scoped directory substitution, Read/Edit tool boundary, and spend cap.
