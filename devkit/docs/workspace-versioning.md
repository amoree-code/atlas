# Workspace versioning (moved)

This document's content was split between:

- **`docs/design/public-private.md`** — *why* local-only versioning matters: the
  permanence of git history, and the asymmetry between a local repository and a remote
  one.
- **`docs/use/safety.md`** — the operational guide: commands, the guard table, the
  snapshot model, and how to undo versioning entirely.

Read those instead. This file is kept as a pointer rather than removed, so a link to the
old path still lands somewhere useful.
