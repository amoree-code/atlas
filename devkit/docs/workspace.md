# Your workspace (moved)

This document's content was split between:

- **`use/workspace.md`** — the directory layout, configuration, templates and
  skills.
- **`use/memory.md`** — memory, knowledge, and how a client reaches the store.
- **`use/projects.md`** — project-scoped state and isolation.
- **`design/workspace-structure.md`** — *why* the workspace is shaped this way.

Read those instead. This file is kept as a pointer rather than removed, so a link to the
old path still lands somewhere useful.
